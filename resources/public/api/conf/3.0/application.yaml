openapi: "3.0.3"

info:
  version: "3.0"
  title: Hello World
  description: |
    # Overview
    Testing various endpoint combinations in the AWS API Gateway
servers:
- url: https://test-api.service.hmrc.gov.uk
  description: Sandbox
- url: https://api.service.hmrc.gov.uk/
  description: Production

components:
  parameters:
    acceptHeader:
      name: Accept
      in: header
      schema:
        type: string
        enum: [ "application/vnd.hmrc.3.0+json" ]
      required: true
    authorizationHeader:
      name: Authorization
      in: header
      schema:
        type: string
        example: Bearer bb7fed3fe10dd235a2ccda3d50fb
      required: true
    year:
      name: Year
      in: path
      description: |
        The year
      schema:
        type: string
        example: 2021
      required: true

paths:
  /hello/world:
    get:
      summary: Hello world
      parameters:
      - $ref: '#/components/parameters/acceptHeader'
      - $ref: '#/components/parameters/authorizationHeader'
      responses:
        201:
          description: "No Content"
      security:
      - {}
      
  /hello/{year}:
    get:
      summary: Hello world with a year
      parameters:
      - $ref: '#/components/parameters/acceptHeader'
      - $ref: '#/components/parameters/authorizationHeader'
      - $ref: '#/components/parameters/year'
      responses:
        201:
          description: "No Content"
      security:
      - {}
